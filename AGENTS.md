# AGENTS.md — 슈국(이수경국어학원) 리포트·시간표·학생 페이지

이 저장소는 학원 운영 페이지의 중심입니다. 학생 개별 페이지(`s.html`), 시간표·출석(`timetable.html`),
숙제 검사(`hwcheck.html`), 내신 피드백(`naeshin.html`), 슈퍼스타(`superstar.html`), 지필 리포트(`m.html`·`r.html`·`t.html`),
공지(`notice.html`)와 Apps Script 백엔드(`backend-createReport.gs`), 수파베이스 마이그레이션(`supabase/`)이 있습니다.
자세한 작업 이력과 결정 사항은 `CLAUDE.md`에 있습니다. 규칙의 근거가 궁금하면 그 파일을 보세요.

## Review guidelines

- 리뷰 댓글은 **한국어**로, 비개발자(학원 원장)도 읽을 수 있게 쉬운 말로 씁니다. 함수 이름은 필요할 때만 한 번 적습니다.
- 학생 데이터가 사라지거나 잘못 바뀌는 문제, 학부모에게 잘못 발송되는 문제, 비밀 키 노출을 최우선으로 봅니다.
- 아래 "Code Review Rules"에 어긋나는 변경은 반드시 지적합니다. 취향 차이(변수 이름, 줄 정리)는 지적하지 않습니다.
- 이 저장소는 프레임워크 없는 정적 HTML + Apps Script + 수파베이스입니다. 빌드 도구·프레임워크 도입을 권하지 않습니다.

## Code Review Rules

### 데이터 원본 (가장 중요)
- **수파베이스가 원본인 데이터를 구글시트 기준으로 통째 덮어쓰는 코드는 금지.** 원본이 수파베이스인 것: 시간표 명단·반(`tt_classes`), 학생정보(`students`), 출석·이동 기록(`attendance`·`tt_log`), 클리닉 신청, 어휘·H WORK 제출, 지필 시험·복기, OMR 회차·응답, 질문 대기열, 공지 확인. 옛 `resyncClasses`·`sbResyncSignup` 같은 "시트 기준 재동기화"를 되살리는 변경은 방금 저장한 이동·신청을 되돌립니다.
- **명단(`tt_classes.roster`)은 읽은 값이 그대로일 때만 씁니다**(`roster=eq.<읽은 값>` 조건의 compare-and-swap, 어긋나면 다시 읽어 재시도). 조건 없이 덮어쓰는 PATCH는 다른 조교의 저장을 지웁니다.
- **중3·고3 정규/내신 짝 반**: 명단·반 시간을 바꾸는 저장(영구 이동·추가·빼기·표기수정·반 통째 이동)은 짝 반에도 함께 적용해야 합니다. 같은 규칙이 두 벌(백엔드 `ttTwinRoster_`, 화면 `rosterOps`/`twinOf`) 있으므로 한쪽만 고친 변경은 지적하세요. 한쪽만 짝이 있으면 건드리지 않는 것이 맞습니다.
- **`tt_classes`를 새로 읽는 화면은 `class_id=not.like.w*` 조건**으로 '이 주만' 반(w+날짜 ID)을 빼야 합니다. 빠지면 전체 시간표에 임시 반이 섞입니다.
- **`tt_log`를 순회할 때는 `isStudentLog`(student가 있고 kind가 1회/주간추가/주간빼기)로 걸러야** 합니다. 반 단위 기록(주간반이동·주간반보강)을 학생처럼 세면 이름 없는 칩·인원수 오류가 납니다. 반이 숨는 날짜는 적용일(`wkApplyYmd`)만 대조합니다.
- `render()` 첫 줄의 `syncWeekClassRosters()` 호출, 옮긴·보강 반의 명단 동기화를 지우는 변경은 지적하세요(과거에 빠져서 옮긴 반이 0명으로 보였습니다).
- '이 주만' 반 ID는 재사용하지 않습니다(`freshWeekId` — tt_log에 흔적이 있는 접미사는 쓴 것으로 봄).
- 학생 제출 경로(s.html·r.html 등)는 **수파베이스 RPC 먼저 → 성공 시 시트 사본을 뒤에서 → 호출 실패 시에만 옛 경로 폴백**입니다. 서버가 판정으로 거절한 것(마감·잘못된 주차·중복)을 폴백으로 다시 넣는 변경은 지적하세요.

### 이름·시간 문자열
- 이름 대조는 **앞 괄호(학교 구분 "(화정)심지후")와 뒤 괄호(특이사항 "김현우(8/23부터)")를 모두 뗀 뒤 정확히 비교하고, 실패하면 끝의 A만 떼고 다시 비교**합니다(B·C는 다른 사람). 이 표준화를 빠뜨리면 같은 학생이 두 줄로 세어집니다. 표기(특이사항)는 마지막 괄호만입니다.
- 시간 문자열을 만들거나 해석하는 코드는 `timeValOf`/`mins`만 씁니다. 12시간 표기를 직접 만들면 아침·밤 10시 이후가 오후로 오해됩니다("오전7:00"·"밤10:00" 접두 규칙).
- 구글시트에 문구·날짜를 쓸 때는 `setNumberFormat('@')`로 텍스트 강제, 읽을 때는 Date 객체도 받아들여야 합니다(시트 자동 변환).

### 백엔드·캐시·비밀
- `backend-createReport.gs` 변경은 재배포가 있어야 반영됩니다. PR 설명에 "재배포 필요"가 없으면 알려 주세요. 새 배포(주소 변경)를 만드는 방식은 금지, 기존 배포 ID를 새 버전으로 올립니다.
- 쓰기 액션이 조회 캐시를 **통째로 비우는** 변경은 지적하세요. 제출이 몰리는 시간에 캐시가 늘 비어 시트 전체 읽기로 돌아갑니다(꼬리 잇기 방식 유지). CacheService 한 칸은 100KB입니다.
- 기록 시트는 꼬리만 읽습니다(`sheetTail_` — 출석 14일·이동 60일 여유). 그보다 먼 미래 날짜로 미리 기록하는 기능을 만들면 여유를 함께 늘려야 합니다.
- **비밀 값(솔라피 API 키·시크릿, 카카오 채널 ID, 템플릿 ID, GitHub 토큰, 수파베이스 service 키)은 어떤 응답에도 담지 않고 저장소에도 커밋하지 않습니다.** 스크립트 속성에만 둡니다. 공개 가능한 것은 수파베이스 publishable 키뿐입니다.
- 카카오 알림톡은 **자동 발송이 없습니다.** 조교가 확인 창에서 [보내기]를 눌러야 갑니다. 확인 없이 보내는 변경, 같은 종류·학생·수업일 중복 발송 방지(dup)를 우회하는 변경은 지적하세요. 템플릿 문구(`ALIM_TPL_`)를 한 글자라도 바꾸면 솔라피 재심사가 필요합니다.
- 같은 규칙이 여러 벌 있는 곳은 함께 바뀌어야 합니다: `STAR_RULES`(백엔드·s.html·superstar.html 세 벌), 공지 대상 판정 `noticeMatches`(s.html·notice.html), 클리닉 대상 판정(클리닉 Code.gs·s.html·clinic_assign.html).

### 화면
- `superstar.html`·`clinic_assign.html`은 **시작 호출을 스크립트 맨 끝**에 둡니다. 중간에 두면 아래의 `var` 선언이 값을 지웁니다.
- 검색 입력창을 입력 중에 다시 만드는 변경은 지적하세요(한글 조합이 끊깁니다). 결과 영역만 갈아 끼웁니다.
- 그리드에 카드를 놓는 코드를 새로 만들면 `spreadOverlaps`를 함께 부르고, 주차별 화면을 다시 그리면 `renderView`가 아니라 `render()`를 부릅니다. 본문 폭을 바꾸는 UI는 폭 전환 뒤 다시 그려야 합니다.
- 수천 줄이 될 수 있는 목록은 한 번에 그리지 않습니다(문자열로 모아 한 번에 넣기·이벤트 위임·나눠 그리기).
- **디자인 규칙**: 폰트는 고운 바탕·고운 돋움·도현체만. UI에 컬러 이모지 아이콘 금지(라인 SVG 또는 글자; 만점 🏆 같은 성취 표시만 예외). 상자 왼쪽에 굵은 색 띠(border-left 강조) 금지. 정해진 색 값(출석 4색, 연보라 #EDE4F4/#8D83A8 등)을 임의 값으로 바꾸지 않습니다.

### 테스트
- `tools/`에 해당 기능의 테스트가 있으면(시간표 짝 반·검색·겹침·강사 필터·카드 높이, 숙제 검사 속도, 알림톡, 이름 변경, 내신 주차, 모의고사 추이, 질문 대기열 SQL) 기능 변경과 함께 테스트도 바뀌어야 합니다. 테스트를 지우거나 건너뛰게 하는 변경은 지적하세요.
