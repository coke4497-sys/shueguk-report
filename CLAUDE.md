# 작업 메모 (이수경국어 · 슈국)

## 브랜드
- 폰트: **고운 바탕(Gowun Batang) + 고운 돋움(Gowun Dodum)**
- 아이콘: 가는 라인 스트로크 SVG, 색상: 세이지 그린 계열
- **UI에 컬러 이모지 아이콘(🔍 ✏️ 등)을 사용하지 않는다** — 버튼·입력창·라벨의 아이콘은 라인 SVG 또는 텍스트만 사용 (사용자 확정 지침, 2026-08-14)
  - 예외: 만점(100%) 표시의 🏆 같은 성취 표시 이모지는 유지 (사용자 확정)
- **출석 상태 색 = 출석 연하늘 · 지각 연노랑 · 결석 연핑크 · 1회 이동 연보라** (색은 사용자 확정, 2026-08-23 — **다만 적용은 사용자 요청으로 보류 중**. "컬러반영 나중에 할게요"). 값·근거는 아래에 다 정해 뒀으니 지시가 오면 그대로 넣으면 된다. 적용 방법: 아래 값을 `timetable.html`(학생 칩·`.att-btns .pick-ok.on`·`.ah-badge.a-*`)과 `hwcheck.html`(`.tt-chip.*`)에 넣는다. 비교 샘플: 세션 아티팩트 '출석 색 바꾸기 전후'. 학생 칩·출석 배지·그래프 어디서나 이 네 가지를 쓴다 — 칩 `#B4D5EE`/`#E9D189`/`#EAB6C2`/`#C4A9E4`, 글자 `#1F5175`/`#5F4E14`/`#7E2B3D`/`#43307A`, 그래프 마크는 같은 계열 진한 값 `#3D82B8`/`#D2941C`/`#C64F63`/`#8E6BC8`(색각 이상 검증 통과 조합).
  - **특이사항 표기는 옅은 회색**(`#C4CDC7`/`#414B45`) — 옛 연하늘 `#E5E9EE`는 출석과 겹쳐서 옮김 (2026-08-23 사용자 확정). 출석 상태가 아니라 이름에 붙은 메모라는 뜻도 회색이 더 맞음.
  - **칩 색은 반 블록 배경보다 두 단계 진하게** (2026-08-23 사용자 확정 — 처음 넣은 옅은 값은 반 배경에 묻혔다). 반 블록 색(`--g1` 연노랑·`--g2` 연보라·`--g3` 연녹·`--mid` 연파랑·`--etc` 연살구)과 칩 색의 지각 색차 ΔE(OKLab×100)가 옅은 값에선 2~4에 그쳐 정리정독(연파랑) 위 출석 칩, 고1 위 지각 칩, 고2 위 이동 칩이 안 보였다. 지금 값은 **모든 반 배경과 ΔE 8.6 이상**, 상태끼리 8.8 이상, 글자 대비 5.2:1 이상.
  - **테두리로 띄우는 방식은 사용자가 거부**(2026-08-23) — 칩에 테두리를 두르지 말고 색 자체를 진하게 할 것.
  - 넷 다 한 벌로 통일 — **컴퓨터/모바일 채도 분리(옛 saturate .35/.75)는 없앴다** — timetable.html의 `@media (max-width:760px)` 칩 색 블록 제거.
  - 출석 체크 버튼(`.att-btns .pick-ok.on`)도 파랑 `#37769F`로 — 누른 색과 만들어지는 칩 색을 맞춤.
- **상자 왼쪽에 굵은 세로 강조라인(border-left 색 띠)을 쓰지 않는다** — "AI 느낌"이라 사용자가 거부 (사용자 확정 지침, 2026-08-23). 제목 상자는 흰 바탕 + 둥근 모서리 + 옅은 그림자만으로 구분한다(shuegukweekendtest `omr_student.html`의 `.title-bar`가 기준 모양). 2026-08-23에 같은 저장소 `signup.html`·`index.html`의 5px 라벤더 띠를 제거함.
  - 구분 대상: 표 열 구분선, 그룹 문항 들여쓰기 표시(`r.html`·`m.html`의 `.in-group`)처럼 **뜻이 있는 얇은 선은 해당 없음**. 금지 대상은 장식용 색 띠.
  - 예외(유지): `timetable.html` 주차별 보기의 센터 라벨 `.wk-ctr`·소제목 `.wk-ch` 왼쪽 선 — 상자가 아니라 글자 옆 얇은 선이고 교사 전용 화면이라 **그대로 두기로 함**(2026-08-23 사용자 확정). 지우지 말 것.

## 정규 시간표 시스템 (2026-08-14 구축)
- **티쳐스 페이지 비밀번호 게이트 전부 제거**(2026-08-14 사용자 확정, 접속 지연 간소화) — timetable·hwcheck·superstar·notice·m·t·analyses·stats + 클리닉(shueguk-clinic teacher.html·clinic_assign.html)·주말 신청(shuegukweekendtest signup_teacher.html) 모두 게이트 없이 바로 열림. 백엔드 TEACHER_PW는 유지하고 페이지에 내장(notice.html과 동일한 공개 수준).
- **교사 페이지**: `timetable.html`
  - 보기: **오늘의 시간표/전체 시간표/주차별/슈국 캘린더** (페이지 이름 '슈국 스케쥴').
  - **슈국 캘린더**(2026-08-14 구축): 달력에서 주마다 [정규]/[내신] 기간을 지정('기간설정' 탭 A:주차수요일 B:구분, 미지정=정규). **오늘의 시간표·주차별은 그 주의 기간에 맞는 시간표를 자동 표시**(상단에 '이번 주 · ○○ 기간' 배지, 수동 선택 불가). **전체 시간표에서만 정규/내신 탭으로 직접 선택.** 날짜 칸 클릭 = 그 날 메모(오늘의 이슈와 같은 '시간표메모' 데이터). API: GET ttPeriodList / POST ttPeriodSet{week,book}.
  - **내신 시간표 데이터**(2026-08-14 세팅): 구글시트 「★2026내신(기본)」 탭에서 116개 반 가져옴(n001~n116). 교재는 반이름에 포함("고2 화수A(동아 문학)"), 확인반 학교 구분은 학생 이름 뒤 괄호("김시은(능곡)"), 시트의 (이예나)는 '이예나(미정)'로 포함. 중3·고3(파이널·정리정독·논술)은 정규와 명단 동일(시트 기준 담당·위치 일부 상이). 같은 시간·같은 반이름에 담당T만 다른 병렬 반(동일 진도 반)은 정상 구조.
  - **중3·고3은 정규·내신 짝 반이 함께 움직인다**(2026-08-25 사용자 요청 "정규 시간표에서 반영한 이동 사항이 내신에서도 동일하게 표시되어야 해요"): 두 시간표는 반ID가 달라(r### / n###) 명단이 서로 남남이었다 — 8/20에 정규에서 한 전민성 영구 이동(고3파이널A 목5:30 → E 일11:00)이 내신에 안 들어가, 내신 주에 옛 반에 이름이 남아 있었다. 이제 **명단·반 시간을 바꾸는 저장은 짝 반에도 같이 적용한다**(양방향 — 내신에서 바꿔도 정규에 반영).
    - **짝 판단 = 반이름 + 요일 + 시작시간**(`ttTwinRowOf_`). 담당T·위치는 시트 기준으로 서로 다른 경우가 있어(정리정독 중3 금8:00 = 정규 은지 / 내신 선주) 짝 조건에 넣지 않고, **같은 이름·시간의 병렬 반이 둘 이상일 때 가려내는 데만** 쓴다(정리정독 중3 토4:30 선주/승연). 그래도 못 가리면 **건드리지 않는다**. 고1·고2·중2는 내신 반이름이 아예 달라('고1 가' ↔ '고1 확인') 짝이 잡히지 않는다 — 그래서 학년 조건을 따로 두지 않았다. 2026-08-25 기준 짝 반 20개(고3파이널A~F, 정리정독 중3 11개, 논술B 국어·수학).
    - 대상 저장: **영구 이동·학생 추가·학생 빼기·표기수정(특이사항)·반 통째 이동**. 백엔드(`backend-createReport.gs`)가 시트 두 탭을 함께 고치고 `twinBook`·`twinMsg`를 응답에 담는다. **1회 이동·주간추가/빼기·주간 반이동 등 '그 주만' 기록은 짝 반영을 하지 않는다** — 오늘·주차별 보기는 그 주의 기간(정규/내신)에 맞는 시간표를 자동으로 쓰므로 애초에 맞는 쪽에 쌓인다.
    - 반 통째 이동은 **요일·시작·끝만** 짝에 옮기고 위치·담당T는 각자 유지. 짝이 정규면 그 반 학생들의 학생정보 정규가/나도 새 시간으로 갱신한다.
    - 표기수정은 짝 쪽 표기가 이미 어긋나 있어도(옛 특이사항이 남아 있어도) **이름으로 찾아 맞춘다**. 짝 반의 최근 60일 출석 기록 이름도 함께 바꾼다.
    - **한쪽만 짝이 있으면 손대지 않는다**(반쪽만 고치면 더 어긋나므로) — 화면에 "○○ 시간표에는 짝 반이 없어 반영하지 못했어요"로 알린다. 반 추가·반 삭제는 짝을 만들지 않으니, 중3·고3 반을 새로 만들 땐 두 시간표에 각각 만들 것.
    - 화면(`timetable.html`)은 `syncTwin(res, names)`로 **짝 시간표의 수파베이스 미러도 다시 맞춘다** — 안 하면 밤 점검 전까지 짝 쪽 화면에 옛 명단이 남는다.
    - 검증: 짝 동기화 로직은 가짜 시트(SpreadsheetApp 스텁)로 8가지 경우를 왕복 검증했다(정규→내신·내신→정규·짝 없는 반·추가/빼기·병렬 반 담당T 구분·표기수정+출석·반 통째 이동·1회 이동 미적용).
      `node tools/tt-twin-test.js`로 다시 돌릴 수 있다. 배포본(리포트 @108)에도 실제로 넣고 빼며 왕복 확인했다.
    - **같은 규칙이 두 벌 있다** — 시트 쪽은 백엔드(`ttTwinRoster_`), 수파베이스 쪽은 화면 어댑터(`rosterOps`/`twinOf`). 명단의 원본이 수파베이스로 바뀌면서(아래) 화면이 직접 짝 반을 고치게 됐기 때문. 한쪽을 고치면 다른 쪽도 같이 고칠 것.
  - **명단의 원본이 수파베이스로 바뀌었다**(2026-08-25 사용자 지시 "명단을 아예 수파베이스로 옮기기"): 영구 이동·학생 추가·빼기·표기수정은 **수파베이스에 먼저 저장**하고 시트에는 같은 액션을 뒤에서 보낸다(학생 개별 페이지·리포트·숙제 검사가 아직 시트 '학생정보'를 읽어서). 반 추가·삭제·통째 이동과 1회 이동은 반ID 생성·결석 보충 판정이 백엔드에 있어 예전대로 기존 백엔드 먼저.
    - 명단은 한 칸에 이어 붙인 문자열이라 통째로 덮어쓰는데, 두 조교가 같은 반을 동시에 고치면 뒤에 쓴 쪽이 앞사람 것을 지운다 — `roster=eq.<읽은 값>` 조건으로 **값이 그대로일 때만 쓰고**(compare-and-swap, `rosterCas`) 어긋나면 다시 읽어 최대 3번 재시도한다.
    - **옛 `resyncClasses`(시트 기준 통째 재동기화)는 지웠다** — 지금 돌리면 방금 저장한 이동이 옛 시트 값으로 되돌아간다. 일일 점검도 tt_classes의 명단은 보고만 하고 요일·시간·담당T만 시트 기준으로 고친다.
    - 검증: `node tools/tt-roster-live-test.js`(실제 수파베이스·시트 왕복). 자세한 것은 `supabase/README.md`.
  - **수파베이스가 갑자기 전부 401이면 장애로 단정하지 말 것** — 2026-08-25에 다른 세션이 학생 데이터 보호(공개 키의 표 권한 회수)를 실행한 것을 장애로 오인해 되돌렸다가 다시 잠갔다. 먼저 `git fetch` 후 `origin/main` 최근 커밋을 확인할 것. 여러 세션이 같은 프로젝트를 동시에 만진다.
    - **2026-08-25에 어긋나 있던 7건은 정규 기준으로 맞췄다**: 전민성 이동(n015→n081, 표기 (8/30부터)), 송치우 논술B 수학(n098)에서 빼기, 채다미(8/21등록) n035에 추가, 고라윤·정수빈·홍창의 옛 특이사항 표기 삭제. 마지막 한 건은 반대로 **정규가 틀렸던 경우** — 정규 시간표가 동명이인을 구분하지 않고 '양지우'로만 적어 두어(명단은 중3 양지우B·고1 양지우A) r001은 양지우B, r026·r044는 양지우A로 고치고 출석 기록 3건의 이름도 함께 정정했다.
  - **주차별 보기 — 한 페이지 개요 + 요일 확대**(2026-08-21 사용자 확정, 샘플 승인 후 적용): 기본은 월~일 7열 개요 — 요일마다 센터(본원→화정센터→민초센터→정리정독, `WK_LOC_ORDER`, 표시명 민트초코→'민초센터') 순으로 묶은 반 요약 카드(시간·인원·출석 요약 숫자, 기록 없으면 —). 요일 머리글/카드 클릭 = 그 요일 확대(센터 섹션 + 학생 칩·출석 기록, `weekBlockEl` 재사용), 나머지 요일은 옆 가는 열, 확대 머리글 재클릭 = 개요 복귀. 옛 30분 그리드(buildWeekGrid)는 제거. 모바일 목록 보기는 그대로.
  - **주차별 보기**: 월~일 달력 순서(월화수목금토일, 날짜 오름차순 — 2026-08-14 사용자 확정). 월·화 열은 그 주 수요일 '직전' 날짜(직전보강용), 수업 주차 자체는 수요일 시작. 지난주/다음주 이동. 그 주의 출석 기록·1회 이동이 반영된 주간 그리드(요일+날짜 헤더). 학생 클릭으로 해당 날짜 출석 소급 기록·수정 가능. **1회 이동은 오늘의 시간표(오늘 적용) + 주차별(대상 반 요일의 그 주 날짜로 적용 — 다음주 여행 등 예약 이동 가능, 2026-08-15)**. 이동기록 J열 '적용일', timetableMove{date}, 오늘의 시간표는 오늘 적용분만 표시.
  - 시간축은 30분 균일 간격. **오늘의 시간표는 그 날 가장 이른 수업 시작부터 표시**(빈 앞줄 제거 — 2026-08-21 사용자 확정, 수업 없으면 평일 2:00/주말 10:00 시작), 끝은 마지막 수업까지 자동 확장. 반이 없는 강사 열·빈 시간 칸도 항상 표시. 오늘·전체·주차별 모두 세로 시간축은 가로 스크롤 시 고정(transform 동기화 — sticky는 브라우저 편차로 미사용), 요일·강사·장소 헤더는 세로 스크롤 시 고정. 오늘의 시간표는 넓은 화면=전체 폭 맞춤(가로 스크롤 없음), 760px 미만(모바일)=열 최소폭 96px 확보 후 가로 스크롤.
  - **이름 뒤 괄호 특이사항**("김현우(8/23부터)", "여선아(입테)" 등)은 칩에 이름만 표시 + 파란색 구분, 마우스 호버 title·출석 모달에서 세부 내용 확인. 명단 데이터에는 괄호 포함 원본 유지.
  - **특이사항 편집(조교 직접)**: 오늘/주차별 = 학생 클릭→출석 창의 '특이사항' 버튼, 전체 = 학생 클릭→반 선택 창(또는 이동바)의 '특이사항' 버튼. 비우고 저장하면 표기 삭제. 백엔드 `timetableRenameStudent{book,from,to}` — 그 시간표의 모든 반(가/나)에서 토큰 교체, '시간표이동기록'에 '표기수정' 로그. 괄호·공백은 자동 제거(명단이 공백 구분이라). **자기 치유**(2026-08-22): 시트 쓰기는 됐는데 수파베이스 미러가 실패하면 화면에만 옛 표기가 남고, 다시 지우려 하면 시트에 그 토큰이 없어 '명단에서 찾지 못했어요' 오류로 영영 안 지워졌다 — 이제 legacy 실패여도 수파베이스 쪽에 그 토큰이 있으면 미러를 실행하고 성공으로 처리한다. (2026-08-22 실제 발생: 유지선(가족모임) r065·r095, 데이터는 시트 기준으로 복구함)
  - **학생 이동 — 영구는 전체 시간표에서, 1회는 오늘의 시간표에서** (사용자 확정 구조):
    전체: 학생 클릭→반 클릭 = 영구 이동(리포트 반영). 오늘의 시간표: 학생 클릭→출석 모달의 '다른 반으로 1회 이동'→반 클릭→사유(필수). 1회 이동 온 학생은 보라색 이름으로 표시, 클릭하면 사유 확인·수정·취소 + 출석 체크. 명단·리포트는 그대로, '시간표이동기록' 탭 기록(8일간 표시).
  - **반 통째 이동**: 반의 '이동' 버튼→빈 칸 클릭. 요일·시간·담당·위치 변경 + (정규) 소속 학생 전원 정규가/나 일괄 갱신.
  - **반 만들기 시간 드롭다운 = 오전 7:00 ~ 밤 12:00, 30분 단위**(2026-08-26 사용자 요청 — 시작은 오전 7:00~밤 11:30, 끝은 오전 7:30~밤 12:00, optgroup 오전/낮/오후/저녁/밤). **시간 표기 규칙이 여기서 확장됐다**: 저장 문자열은 여전히 12시간 표기("5:30")가 기본이되, 그 표기로 구분이 안 되는 시간대만 접두를 붙인다 — **아침(10시 전) `오전7:00` · 22시부터 `밤10:00`~`밤12:00`**. 직렬화는 `timeValOf(분)` 한 곳(timetable.html), 해석은 `mins()`가 세 표기를 다 읽는다(밤12:00=1440). 기존 데이터·기존 범위 표기는 바뀌지 않음(왕복 검증: scratchpad 테스트로 7:00~24:00 전 구간 mins(timeValOf(m))===m 확인). 반 통째 이동(dropClass)·폼 프리필도 timeValOf 경유 — 프리필은 분으로 환산해 넣으므로 옛 끝 표기 "10:00"(=밤10시)은 `밤10:00` 칸에 맞게 선택된다. 검증문도 밤 12시 한도로 완화(4곳, '바뀐 것 없음' 비교는 분 단위로). 백엔드는 무변경(시간 열은 이미 `setNumberFormat('@')` 텍스트 강제라 "오전7:00"이 날짜로 안 바뀜). hwcheck `slotSortKey`도 접두 표기 해석 + 9시(밤) 정렬 보정. **새로 시간 문자열을 만들거나 해석하는 코드는 반드시 `timeValOf`/`mins`를 쓸 것** — fmt12로 직접 만들면 아침·밤 10시 이후가 12시간 표기로 저장돼 오후로 오해된다.
  - **편집 도구줄**(전체·주차별 상단): 반 이동/학생 추가/반 삭제/반 추가/이동 기록 — 반 블록 안에는 버튼 없음(사용자 확정). 학생 빼기는 학생 클릭 후 '반에서 빼기'. 모두 기록 남음. 새친구는 슈퍼스타 관리에서 신입 등록 후 추가.
  - **오늘의 보충 방문 팝업**(2026-08-26 사용자 요청 "영상보충의 경우 해당 날짜의 오늘의 시간표에서 팝업으로"): 결석자 관리의 영상·대면 보충 일정("영상보충: 8/26(수) 6:00") 중 **적힌 날짜(M/D)가 오늘인 건**을 오늘의 시간표에서 보여 준다 — 도구줄에 '오늘 보충 방문 n건/없음' 버튼(`#a-vm`) + 팝업(`openVmToday`)에서 학생·원 결석·일정·메모 확인과 [영상보충 출석] 완료 처리(`vmDone`). **있든 없든 하루 한 번은 저절로 뜬다** — 없는 날은 "오늘은 영상보충 올 친구가 없습니다." (2026-08-26 사용자 확정 "없는 날에도 팝업을"; localStorage `tt_vm_seen`, 다른 창이 열려 있으면 방해 안 함, 다 확인한 뒤엔 '모두 확인했어요' — `vmHadAny`로 구분). 데이터는 정규+내신 attendAbsentList를 오늘의 시간표에서만 조회(`vmEnsure`, [다시 불러오기]가 다시 조회). 날짜 대조는 plan 문구의 첫 M/D(`vmPlanMD`) — 연도 없이 비교하므로 1년 전 같은 날짜 건이 남아 있으면 잡힐 수 있음(드묾).
  - **오늘의 시간표**: 오늘 요일 수업 + 1회 이동 반영 상태로 표시, 학생 클릭→**출석/지각/결석** 기록(지각은 메모 필수). '출석기록' 탭에 날짜별 저장 — 구글시트의 주차별 탭(8/12주차 등) 역할.
  - **지필고사 일정**(2026-08-21 사용자 확정, 시안 승인 후 적용 — 리포트 배포 @103): 슈국 캘린더 아래 접이식 '지필고사 일정' 섹션 — 구글시트처럼 날짜가 위, 학교별 색 막대가 아래로 쌓이는 타임라인(겹침 확인), 고등학교/중학교 그룹 + [전체/고등/중등] 필터. **시즌(시작일 14일 이내 묶음) 단위 표시: 시즌 시작 45일 전(내신 대비 기간)부터 끝나는 달까지 시즌 전체가 함께 보임**(중간 9/17~10/15 → 8·9·10월, 기말 12월 → 10·11·12월, 10월엔 둘 다). 2학기 중간고사 20개교 기간 입력 완료(시트 색 띠 기준, 사용자 검수). **학교별 시험 기간(시작~끝)을 직접 입력**(시험 시간표 공지 전엔 기간만 아는 경우가 많음 — 중학교 포함), 시간표가 공지되면 '+ 날짜별 과목 추가'로 기록 → 달력 날짜 칸에 학교색 칩 표시. '지필일정' 탭(A:유형(기간/과목) B:학교 C:시작일 D:종료일 E:내용 F:기록일시), API GET examSched{from,to} / POST examSchedSet{row?,del?,kind,school,start,end,text}. 학교 색은 base 이름(SCHOOL_HUE, 구글시트 색 계승) + 해시 폴백, 접기 상태 localStorage(tt_exs_open). **가독성 개선(2026-08-21, "보기에 불편" 피드백)**: 타임라인 축 3줄(월·날짜·요일, 학교 8곳 넘으면 아래에도 축 반복), 주말·오늘 세로 띠 + 한 줄 건너 격줄 배경, 학교명 열은 가로 스크롤에 고정(sticky, 이름 아래 작은 기간 표기 — 모바일에서 스크롤 없이 기간 읽힘), 월 이름도 스크롤 시 시야에 남음. 시즌이 여럿 보이는 달(10월 = 중간+기말)은 타임라인을 시즌별로 분리(캡션: 기간 메모 최빈값 + 날짜 범위, `exsVisibleClusters`). 접힌 헤더에도 '첫 시험 M/D(요일) · D-n' 또는 '시험 기간 중' 표시. **막대 아래 과목 칸(구글시트 구성 재현, 2026-08-21)**: 학교마다 막대 줄 아래 흰 상자 줄 — 시험 시간표가 나오면 **빈 날짜 칸 클릭 = 그 학교·날짜가 미리 채워진 과목 입력 창**(exsEditSubject(row, school, date)), 입력하면 그 칸에 학년·과목 표시(기간 밖 날짜 과목도 개별 상자로 표시). 과목 수정 창에 삭제 버튼 추가. 아래 '날짜별 과목' 목록은 날짜별 묶음으로 바꾸고 **한 날짜에 2개교 이상이면 'n개교 겹침' 붉은 표시**(직전보강 몰리는 날 파악용 — 사용자 요청 배경). **별도 메뉴로 승격(2026-08-21 사용자 확정)**: 슈국 캘린더 아래 접이식이 아니라 **뷰 탭에 '지필고사 일정' 버튼**(슈국 캘린더 옆, mode='exs'). 데이터는 앞뒤 1년 로드(`exsLoadAndRender`), **시즌 버튼**(2학기 중간/기말 등 — 시작월로 이름 추정: 3~7월=1학기·5월까지 중간, 8~2월=2학기·10월까지 중간, 같은 이름 두 해면 'yy년' 접두)으로 골라 보기, 기본 = 진행 중/다가오는 시즌, 선택은 sessionStorage(tt_exs_season). **슈국 캘린더 연동**: 날짜 칸 과목 칩을 '화정고1 국어' 형태(학교base+학년 붙임, 쉼표 구분 항목마다)로, **그 학교 시험 시작일이면 붉은 '첫째날' 배지**(calExamChip — 날짜별 과목 목록에도 동일 배지). 접이식 exsOpen/exsToggle·tt_exs_open은 제거. **과목명 두 글자 축약(2026-08-21 사용자 확정 — "칩 자리 부족" 대응)**: 표시 전용 `subjAbbr` — 두 단어 이상이면 단어 첫 글자 조합('화법과 언어'→화언, '독서 토론과 글쓰기'→독토, '독서와 작문'→독작 자동), 예외 목록 SUBJ_ABBR(공통국어*→국어, 심화국어→심국, 고전읽기→고전), '언매/화작'은 각각 축약. 달력 칩은 **항목마다 학교+학년을 분명하게**('능곡고1 국어, 능곡고2 화언' — 학교 이름 한 번만 쓰는 압축은 사용자가 거부, 2026-08-21), 막대 아래 칸은 학년+축약만(`exsCellAbbr`, '고1 국어·고3 독토' — 학교가 줄 이름에 있어서 허용). **저장 원문은 그대로**(날짜별 과목 목록·title 호버에 원문). 최악(하루 6개교) 컴퓨터는 여유, 모바일도 칩당 2~3줄로 전부 보임 — 개수 제한 없이 다 보여주는 방식(사용자 '한눈에' 요구). **휴대폰(<760px) 달력은 요약 배지**(2026-08-21 사용자 확정): 날짜 칸에 [시험 n곳][첫째날 n][메모] 배지만 표시(칸 높이 일정), **칸을 누르면 팝업**(`calDayPopup`)으로 학교별 상세(첫째날 표시·원문 과목·수정 버튼)+메모(쓰기/수정 버튼) — 시험·메모 없는 날은 기존대로 바로 메모 창. 컴퓨터 화면은 칩 그대로.
  - **주차별 = 그 주에만 적용되는 변경**(2026-08-23 사용자 요청 "이 변경 사항은 이 주차에만 해당해요"): 주차별에서 하는 출석 수정·학생 추가·빼기·이동·반 만들기는 **전부 그 날짜 하루에만** 적용된다. 정규/내신 명단(tt_classes)·다른 주차·전체 시간표·학생 개별 페이지는 그대로.
    - 저장 구조: **새 테이블 없이 `tt_log`에 종류만 다르게 쌓는다** — `주간추가`(to만)·`주간빼기`(from만)·`주간반추가`·`주간반삭제`, 적용일은 `apply_date`. 어댑터 액션 `timetableWeekAdd`/`timetableWeekRemove`/`timetableWeekCancel`/`timetableWeekClass`/`timetableWeekClassDel` — **수파베이스 전용**(시트에 대응 기능이 없음, 지필일정·1회 이동 수정과 같은 취급).
    - 읽기: `fetchOnceLogs(base, kinds)`가 종류를 골라 받는다. ttBoot·timetableWeek만 `WEEK_KINDS`(1회+주간추가+주간빼기)로 받고, attendStudent·attendWarnList는 1회 이동만 세도록 그대로 뒀다.
    - 화면: 주간추가 학생은 `이름 · 이 주` 보라 칩(출석 체크 가능), 주간빼기 학생은 원래 칩이 흐려지며 `이 주 빠짐`. 인원수는 `3-1+1명`. 칩을 누르면 되돌리기(주차별에서만).
    - **그 주에만 있는 반**: 반ID를 `w`+yyMMdd+한 글자(`w260819a`)로 만들어 **날짜를 ID에 담는다** — 스키마 변경 없이 그 날짜에만 보이게 하려는 선택. 카드는 점선 테두리 + `이 주만` 배지, 카드(이름 부분)를 누르면 학생 넣기·반 지우기. `wkVisible(c, dstr)`가 날짜를 안 주면(전체 시간표) 숨긴다.
      - **주의**: 이 반은 시트에 없으므로 `resyncClasses`의 정리 DELETE에서 빼 뒀다(`class_id=not.like.w*`). 같은 이유로 hwcheck·naeshin·m 페이지의 `tt_classes` 조회에도 같은 조건을 넣었다 — 새로 tt_classes를 읽는 화면을 만들면 이 조건을 꼭 넣을 것.
    - 편집 도구줄이 화면에 따라 갈린다: 전체 = [반 이동][학생 추가][반 삭제][반 추가], 주차별 = [이 주만 학생 추가][이 주만 반 추가](`#eset-all`/`#eset-week`).
    - **반 전체를 그 주만 다른 시간으로**(2026-08-23 사용자 질문 "반 전체를 1회만 다른 시간으로 이동"): 주차별에서 보통 반 카드를 누르면 [이 주만 시간 옮기기]/[이 주만 학생 넣기] 메뉴. 옮기면 같은 이름·명단의 **복사본을 새 시간에 주간 전용 반으로 만들고**(`timetableWeekClassMove`), `주간반이동` 기록으로 **원본을 그 날짜에만 숨긴다**(`wkMovedAway`). 복사본을 지우면(`timetableWeekClassDel`) 그 기록도 함께 지워져 원래 시간으로 돌아온다 — 버튼 이름도 '원래 시간으로 되돌리기'로 바뀐다(`wkMovedFrom`).
      - **이동해 나간 자리는 흐린 카드로 남는다**(2026-08-26 사용자 요청 "이동한 자리에 연하게 어디로 이동이라는 표시"): 옛날처럼 감추지 않고(`wkVisible`이 이제 주간반이동을 숨기지 않음) 휴강 카드(`.wkoff`)와 같은 흐림+취소선으로 원래 자리에 두고 `→ 9/12 (토) 오전9:30~11:00 · ○○T 에서 해요`를 적는다(`wkMovedToLabel` — 날짜는 복사본 반ID에서 `wkIdYmd`). 주차별 카드(weekBlockEl)·오늘의 시간표(blockEl '오늘 이동')·주간 개요 미니 카드(`.wk-mini.mvd`, 요약 숫자 대신 '이동')·옆 요일 막대까지 동일. 카드를 누르면 안내 팝업(`openWeekMovedInfo` — 사유·되돌리는 곳, 주차별에서는 [옮긴 수업 보러 가기]=`wkGotoMoved`가 그 주·그 요일로 이동).
      - **복사본 명단은 원본 반을 그대로 따라간다**(2026-08-23 사용자 확정 "반 전체를 옮기는 경우 명단이 전체 시간표와 동일해야 해요"): 만들 때 명단을 베끼지 않고(`roster: ''`), 그릴 때마다 `syncWeekClassRosters()`가 `주간반이동` 기록의 원본 반에서 지금 명단을 가져와 넣는다(`render()` 첫 줄). 옮긴 뒤 전체 시간표에서 학생을 넣거나 빼도 그 주 수업에 바로 반영된다. 그 주에만 넣고 뺀 학생(`주간추가`/`주간빼기`)은 지금처럼 따로 표시된다.
    - **반 전체를 다른 주로 당기기/미루기**(2026-08-23 사용자 요청 "10/5주차의 수업을 8/24주차에 당겨서 하는 것을 주차별에서 기록"): [이 주만 시간 옮기기] 창에 **'옮길 주' 선택**(`weekSelHtml` — 이번 주~15주 뒤+지금 보는 주, 값=그 주 수요일, 정규/내신 구분이 지금 화면과 다른 주는 잠금). 다른 주를 고르면 복사본이 그 주 날짜의 주간 전용 반(`w`+날짜)으로 만들어지고, 원본은 **지금 보는 주 날짜에만** 숨는다(기존 `주간반이동` 구조 그대로 — apply_date=원래 날짜, 복사본 날짜는 반ID에). **조작은 원래 수업이 있는 주차에서**(숨김이 그 주에 걸리므로 — 10/5주차 화면에서 옮길 주=8/24주). 되돌리기는 당겨 간 주의 복사본 카드에서('원래 시간으로 되돌리기', 창에 '○○ 수업을 이 날로 옮겨 온 거예요' 표시). 어댑터: 이동·보강 기록은 **적용일과 복사본 날짜 중 하나라도 기간에 들면 조회 포함**(`copyClassYmdOf` — timetableWeek·ttBoot 주간·ttBoot 오늘 8일 창), 주간 기록(WEEK_KINDS) 조회 창 68→**120일**(`fetchOnceLogs` days 인자 — 최대 15주 앞 예약이 그 주를 볼 때까지 살아 있게). 실제 왕복 테스트 통과(2026-08-23: 저장→양쪽 주 조회→ttBoot→되돌리기→뒷정리, 먼 미래 주차 사용).
    - **반 전체 보충**(2026-08-23 사용자 요청 "이 반 전체를 추가 보충"): 같은 창의 [이 주만 보충 추가] — 원래 수업은 **그대로 두고** 같은 반을 그 주에 한 번 더 연다(`timetableWeekClassExtra`, 기록 `주간반보강`). 이동(`주간반이동`)과 만드는 방식은 같고(`weekClassCopy`) **원본을 숨기지 않는 것**만 다르다. 명단은 이동과 마찬가지로 원본 반을 그대로 따라간다. 지우기 버튼은 이동이면 '원래 시간으로 되돌리기', 보강이면 '이 보충 지우기'.
    - **`render()` 첫 줄의 `syncWeekClassRosters()` 호출을 지우면 학생이 안 따라온다**(2026-08-23 사용자 신고 "학생들이 따라오지 않아요"로 발견): #302에서 넣은 그 한 줄이 **#307(출석 현황 개편)에서 빠져** 그 뒤 배포본에서는 옮긴·보충 반이 계속 0명으로 보였다. 명단은 DB에 없고(`roster: ''`) 그릴 때마다 이 함수가 원본 반에서 채우는 구조라, 호출이 사라지면 조용히 빈 반이 된다. 브라우저 E2E가 이 경로를 검사한다.
    - **[이 주만 반 추가]에서도 기존 반을 고를 수 있다**(2026-08-23 사용자가 빈 반을 만들고 학생을 한 명씩 넣고 있던 것을 기록에서 발견 — 8/29 백양A·B·C에 24명 수작업): 창 맨 위 '어느 반 수업인가요?'에서 반을 고르면 이름·시간·담당·위치가 자동 채워지고 **명단이 그대로 따라온다**(원본 수업 처리: 보충 = `timetableWeekClassExtra` / 옮기기 = `timetableWeekClassMove`). '새 반 만들기'를 고르면 종전대로 빈 반. 이미 만들어 둔 '이 주만' 반에는 카드 메뉴의 **[다른 반 학생 한꺼번에 넣기]**(`openWeekBulkAdd` — 반 선택 → 전원 `주간추가`, 이미 들어온 학생은 건너뜀).
    - **이 주만 휴강**(2026-08-23 사용자 질문 "이 반을 이 주만 삭제하고 싶을 때"): 보통 반 카드 메뉴에 [이 주만 휴강] — 반은 그대로 두고 **그 날짜에만 수업을 없앤다**(`timetableWeekClassOff`, 기록 `주간반휴강`, 되돌리기 `timetableWeekClassOn`이 그 기록을 지움). 옮기기(`주간반이동`)에서 복사본만 안 만드는 형태. **감추지 않고 '이 주 휴강' 카드로 남긴다**(`.blk.wkoff` 흐림+취소선, `wkOffAt`) — 왜 수업이 없는지 보이고 카드를 눌러 되돌릴 수 있어서. 오늘의 시간표에서도 '오늘 휴강' 카드로 나오되 **학생 칩이 없어 출석을 찍을 수 없다**(`blockEl` 앞부분에서 처리).
    - **'이 주만' 반 ID는 한 번 쓰면 재사용하지 않는다**(2026-08-26 사용자 신고 "반을 이동하면 이름이 중복으로 따라옵니다"로 발견): 접미사 할당이 살아 있는 tt_classes만 봐서, 손으로 만든 8/29 백양A(w260829a, 주간추가 9명)를 지우고 n032를 당겨오자 **지워진 반과 같은 ID가 재사용**돼 옛 주간추가 9건이 새 복사본에 되붙었다(명단 동기화 9명 + 유령 9명 = 9+9명). 세 겹으로 고침 — ① `freshWeekId`(weekClassCopy·timetableWeekClass 공용): tt_classes에 없어도 **tt_log에 흔적이 남은 접미사는 쓴 것으로 본다**(주간반삭제 기록이 ID를 영구히 표시), ② `timetableWeekClassDel`이 그 반을 가리키던 **주간추가·주간빼기 기록도 함께 삭제**, ③ `dedupTemps(c, temps, awayOf)`: 명단에 이미 있는 학생은 '이 주'·'1회' 칩을 만들지 않는 렌더 방어(weekBlockEl·blockEl, away 학생 재추가는 유지) + openWeekAdd에 명단 중복 사전 차단. 남은 유령 기록 18건(212~235, w260829a·w260829c)은 dedupTemps로 화면에 안 나온다 — 데이터 삭제는 권한 승인 필요해 보류(사용자가 지우라 하면 tt_log에서 `kind=주간추가 & to_class_id in (w260829a,w260829c)` 삭제).
    - **반 단위 기록을 학생 칩으로 오해하던 문제**(2026-08-23 브라우저 테스트에서 발견): `주간반이동`·`주간반보강` 기록은 student가 비어 있고 from/to에 반ID만 있는데, `weekBlockEl`·`onceOutThisWeek`·`onceInToday`가 종류를 안 가리고 순회해 **옮겨 간 반에 이름 없는 `· 1회` 칩**이 생기고 인원수가 `5+1명`으로, 보강의 경우 원본 반이 `5-1명`으로 어긋났다. `isStudentLog(o)`(student가 있고 kind가 `1회`/`주간추가`/`주간빼기`)로 거른다 — 새로 tt_log를 순회하는 화면을 만들면 이 함수를 쓸 것.
    - **변경사항 목록에 요일·시간 표시**(2026-08-23 사용자 지적 "고2확인→고2확인 알아보기 어렵다"): 병렬 반은 이름이 같아 구분이 안 돼서 반ID로 요일+시작시간을 찾아 붙인다(`chClassIndex`/`chClsLabel`). 색인은 `classes` + `localStorage`의 `list:정규`·`list:내신` 캐시에서 만들고, 없으면 한 번 받아 와 다시 그린다(`chFillClassIndex`). 검색어도 이 시간 문자열까지 훑는다.
    - **실제 왕복 테스트 완료**(2026-08-23): 어댑터를 그대로 실행해 저장→조회→되돌리기→뒷정리까지 확인. 그 과정에서 `fetchOnceLogs`의 68일 창이 **기준일이 미래면** 방금 넣은 기록을 잘라내던 문제를 찾아 고침(기준일이 오늘보다 뒤면 오늘 기준으로 조회).
  - **전체 시간표 변경 → 학생 개별 페이지 연동**(2026-08-23 사용자 요청): 영구 이동·추가·빼기·반 통째 이동이 끝나면 `resyncClasses(book, names)`가 바뀐 시간표에서 정규가/나를 다시 계산해 **수파베이스 `students` 미러**를 갱신한다. 그전엔 시트 '학생정보'만 갱신되고 미러는 밤 점검 때까지 옛 반이 남아 s.html 시간표가 하루 뒤처졌다. 이름이 유일할 때만 반영(백엔드 `ttRosterSet_`와 같은 규칙), 내신은 원래 학생정보에 반영하지 않는 규칙이라 그대로.
  - **화면마다 할 수 있는 일을 나눔**(2026-08-23 사용자 확정): **오늘의 시간표 = 출석 체크만**, **주차별 시간표 = 1회 이동 기록만**, **전체 시간표 = 영구 이동만**. 각 화면 안내문(`.hint`)에 "여기서는 ○○만 합니다 + 나머지는 어디서" 라고 명시(innerHTML, `.hint b`·`.hint-x` 스타일).
    - 출석 창(openAttend)에서 [다른 반으로 1회 이동] 버튼 제거, `startOnceMoveById` 삭제. 오늘의 시간표에서는 1회 이동을 **시작할 수 없다**(onBlockClick의 `moving.once` 분기 제거).
    - **주차별은 지난 날짜 출석 수정도 한다**(2026-08-23 사용자 요청 "이전 일자의 출석 사항도 수정할 수 있어야 해요"): 학생 칩 클릭 = 그 날짜의 `openAttend(c, nm, dstr)`, 창 안 [다른 반으로 1회 이동] 버튼(`startOnceMoveById`)으로 1회 이동을 시작한다. 그 버튼은 주차별에서만 보인다(오늘의 시간표는 출석 체크 전용 유지).
    - `openOnceManage`(1회 이동해 온 학생)는 오늘·주차별 **모두** 출석 버튼을 보여 준다(지난 날짜 수정용). 사유 편집·취소는 주차별에서만.
    - 안내문: 주차별 = "지난 날짜 출석 수정과 1회 이동을 합니다", 오늘 = "오늘 출석".
  - **변경사항 패널**(2026-08-23 사용자 요청): 주차별·전체 시간표 **오른쪽에 접었다 폈다** 하는 고정 패널(`#chpanel`, 함수 접두 `ch`, 스타일 `.ch-*`). 옛 편집 도구줄의 [이동 기록] 모달(`openLog`)을 대체 — 버튼 이름도 '변경사항'으로 바꾸고 `chOpenFromBar()`가 연다. 접힘 상태는 `sessionStorage tt_chopen`, 세로 탭(`.ch-tab`)을 눌러 여닫는다. 다른 보기(오늘·캘린더·지필)에서는 `hidden`. 데이터는 기존 `timetableLog` 그대로. **시간표를 바꾸는 저장(`CH_ACTIONS` 목록)이 성공하면 `chDirty()`로 자동 갱신**. 넓은 화면(≥1100px)에서는 열릴 때 `body.ch-on`으로 본문에 오른쪽 여백을 줘 상단 버튼이 가려지지 않게 하고, 좁은 화면은 그대로 덮는다.
  - **변경사항 검색**(2026-08-23 사용자 요청): 패널 머리글 아래 검색 줄(`.ch-find`) — 돋보기 SVG + 입력창(`#ch-q`)과 종류 칩(`#ch-kinds`). 검색은 이름·반(전/후)·사유·날짜·종류·구분(정규/내신)을 한 문자열로 합쳐 부분 일치, 종류 칩은 **실제 기록에 있는 종류만** 동적 생성(전체/이동/1회/빼기/추가/표기수정, 종류가 하나뿐이면 칩 줄 자체를 감춤)이고 같은 칩을 다시 누르면 전체로 돌아온다. 머리글 건수는 거르는 중이면 'n / m건'. 받아 둔 목록(`chRows`)을 화면에서 거르므로 **서버 왕복은 그대로 1회**, `chLoad`는 저장/새로고침 때만. 함수 `chKinds`/`chSetKind`/`chRender`. **[지난 기록 더 보기]**(2026-08-26 사용자 요청 "100건만 확인 가능"): 목록 맨 아래 버튼(`.ch-more`) — 처음 100건, 누를 때마다 200건씩 늘려 다시 받는다(`chLimit`/`chMore`, 어댑터 `timetableLog`가 `limit` 파라미터(최대 2000)와 `more` 플래그 지원, 스크롤 위치 유지 `chKeepScroll`). 받은 수가 요청한 수보다 적으면 버튼이 사라진다.
  - **출석 현황**(2026-08-23 사용자 요청 — 옛 [학생 이력]·[결석/지각 확인] 두 모달을 하나로 합침): 상단 '출석 관리' 묶음은 이제 **[결석자 관리][출석 현황]** 두 개. [출석 현황]은 `mode='att'` 전체 화면 — 기간(4/8/12주) × 보기(**전체/학년별/학교별/개인**) × 정렬(문제 많은 순/출석률 낮은 순/이름순) + 이름 검색.
    - **구글시트 「주차별 기록부」 형식**(2026-08-23 사용자 제시 이미지 기준): No · 이름 · 주차마다 **[진도][확인][보충 및 이동]** 세 칸. 칸에 들어가는 값은 상태 글자가 아니라 **그 주에 들은 수업의 요일+시간**('일2:00'), 이상이 있을 때만 색과 괄호 메모로 표시한다.
      - 검정=제 시간 출석 / 노랑=지각+`(메모)` / 빨강=`결석`+`(사유)` / **파랑=1회 이동(옮겨 간 반의 시간)** / 옅은 회색=아직 출석 체크 안 함 / 빈칸=그 시간표에 그 수업이 없음.
      - **진도/확인은 그 주의 시간표로 가른다** — 내신 주는 진도반/확인반(반이름에 '확인'), 정규 주는 가/나. 주차 머리글에 기간(정규/내신)을 함께 적는다.
      - **중3·고3은 2학기에 내신 시간표가 정규와 같아 진도 라인에만 표시하고 확인 칸은 비운다**(2026-08-23 사용자 확정). 슬롯을 '반 하나'가 아니라 '반 목록'으로 들고 있어 확인반이 없으면 자연히 빈칸이 된다.
      - 보충 및 이동 칸 = 그 칸에 못 담은 이동 + 결석 보충 계획(`attendAbsentList`의 plan). 칸에 이미 파랑으로 보인 이동은 중복해 넣지 않는다.
      - 줄(이름)을 누르면 그 학생 날짜별 상세가 펼쳐진다 — 옛 학생 이력이 여기로 들어옴. 옛 결석/지각 확인은 '문제 많은 순' 정렬 + '주의 학생' 타일로 대체.
      - **직보(직전보강)**: 시험 직전 주에는 기존 수업이 사라지고 직전보강 시간표로 바뀐다(2026-08-23 사용자 설명). 아직 그런 반이 시간표에 없어서, **반이름에 '직보'·'직전'·'보강'이 들어오면 자동으로 초록 표시**(`asIsJikbo`)되게 해 뒀다. 시험 주간에 그 반을 만들면 그대로 잡힌다.
      - **1회등원**: 주2회 수업인데 그 주에 한 번만 등원한 경우(드물게 있음 — 사용자 설명). **지나간 주**에서 두 칸 다 수업이 있는데 한쪽에만 기록이 있으면 나머지 칸에 옅은 회색 '1회등원'. 이번 주는 아직 체크 중일 수 있어 판정하지 않는다.
      - **퇴원**: 재원 명단에도 없고 시간표에도 없으면 퇴원으로 본다(미등록생은 시간표에는 있다 — 그래서 둘이 갈린다). 이름 아래 '퇴원'으로 표시하고, **그 기간에 기록이 남아 있을 때만 목록에 남긴다**(기록이 빠지면 자연히 사라짐).
    - **결석·지각 목록**(보기 알약의 다섯째 — 옛 [결석/지각 확인] 모달과 같은 확인): 기간 안에 지각·결석·1회 이동이 있는 학생만 잦은 순 랭킹. 기준 [종합/지각/결석/1회 이동] 전환, 합 3건 이상은 붉은 강조, 줄을 누르면 날짜별 내역. 보충 진행은 `보충 n/m` 배지로. 함수 `asWarnHtml`, 스타일 `.as-w*`.
    - **대상 학생 타일에 구성을 적는다** — '재원 496 · 미등록 2 · 퇴원 1'(`asCntNote`). 재원 명단보다 수가 큰 이유(아직 등록 전인 미등록생, 명단에서 빠졌지만 기간 안에 기록이 남은 퇴원생)가 바로 보이게.
    - 위에 요약 타일(대상 학생·출석률·지각·결석(보충)·1회 이동·주의 학생)과 **주차별 지각·결석·이동 숫자 표**(2026-08-23 사용자 요청 "막대 그래프 말고 표(숫자)로" — 옛 `.as-trend` 막대는 제거). 줄=지각/결석/1회 이동/합, 열=주차+합계.
    - **기간 합계 열은 없앴다**(2026-08-23 사용자 요청 "기간 합계 불필요") — 기록부 형식으로 바뀌며 안 쓰이던 옛 점 표(`asHead`/`asCell`/`asRowHtml`)도 함께 제거.
    - **학년별·학교별은 그룹 요약 줄**(눌러서 접힘, 그룹당 6명 미리보기 — 알약에서 학년/학교 하나를 고르면 전체 명단). 출석률은 그룹마다 비슷해 막대는 **수업 100회당 지각·결석 건수**로 그려 그룹끼리 비교되게 했다.
    - **데이터: 백엔드 변경 없음** — `timetableWeek`(정규+내신)+`roster`+`attendAbsentList`+`timetableList`를 화면에서 집계(`asBuild`). 2026-08-23 기준 출석기록 930건(8/8 시작)이라 가볍다. **기록이 쌓여 느려지면 서버에서 학생별·주차별로 집계해 주는 액션을 따로 만들 것.**
    - **이름 대조**: 출석기록엔 이름만 있어 재원 명단과 이어 붙인다. **이름 앞뒤 괄호는 모두 떼고 대조한다** — 뒤 괄호는 특이사항("김현우(8/23부터)"), **앞 괄호는 내신 확인반의 학교 구분**("(화정)심지후" — 2026-08-23 사용자 지적으로 발견, 앞 괄호를 안 떼서 명단에 있는 학생이 '미등록생'으로 잘못 잡혔다). `plainName`/`nameNote`(timetable) · `plain`(naeshin) · `plainNm` 등(hwcheck) · m.html 배정까지 같은 규칙으로 맞춰 뒀다. 명단에 없는 학생은 **아직 등록 전인 미등록생**이라 '(미등록생)' 그룹으로 따로 묶어 맨 아래 둔다 — 오류가 아니다(2026-08-23 사용자 확인: 김강우·김남우가 그런 경우). 동명이인 표기가 어긋나므로(시간표는 첫 사람도 `양지우A`, 명단은 `양지우`) **정확히 일치 → 실패하면 끝의 A만 떼고 재대조**(`asKey`). B·C는 명단에도 있어 떼지 않는다(떼면 다른 사람에 붙는다). **이 표준화는 이름을 세는 모든 곳에 적용한다** — 재대조만 하고 키를 그대로 두면 '양지우A'(시간표)와 '양지우'(명단)가 **두 줄로 갈라져** 한 사람이 둘로 세어진다 (2026-08-23 사용자 질문 "496명이라고 되어있었는데 왜 500명이에요?"로 드러남 — 재원 496명이 498명으로 부풀어 있었다). 명단에 없는 학생은 '(학년 미상)' 그룹으로 드러난다.
    - **일요일 논술 특강은 집계 제외**(`asIsNonsul` — 요일 '일' + 반이름에 '논술'). 제외 건수는 안내 줄로 표시.
    - 함수 접두 `as`, 스타일 `.as-*`(상세는 옛 `.ah-*` 재사용, `ahMkBadge`도 남겨 씀). 옛 `ah*`/`aw*` 모듈은 제거.
  - **전체 시간표 — 한 페이지 개요 + 요일 확대**(2026-08-22 사용자 요청 "주차별처럼 슬라이드 없이"): 기본 화면이 주차별과 같은 월~일 7열 개요(가로 스크롤 없음) — 요일마다 센터 순 반 요약 카드(반이름·시간·담당T·인원, `wk-*` 스타일 재사용). 요일 머리글/카드 클릭 = 그 요일 확대(`buildAllZoom`, 반 블록 `blockEl` 그대로 — 학생 클릭 영구 이동·편집 도구줄·검색 강조 모두 동작), 확대 머리글 재클릭 = 개요 복귀. **확대 화면은 센터별 시간 그리드**(2026-08-22 사용자 확정 시안 A — "타임라인별 수업을 한눈에"): 왼쪽 30분 눈금 + 강사 열, 카드는 실제 시작·끝 위치와 길이대로 배치(`zoomCenterGrids`/`zoomTuneRows`, 행 높이는 카드가 잘리지 않게 자동 상향, 최대 120px). 센터마다 강사 3열 안팎이라 가로 스크롤 없음. 빈 칸은 반 이동 드롭 자리로도 동작. **모바일(<760px)**: 옆 요일 열이 폭을 다 먹어 그리드가 눌리던 문제 → 옆 요일은 위에 한 줄(`flex-wrap`), 그리드는 전체 폭 + 가로 스크롤(열 최소폭 `--zcol:112px`). 옛 30분 그리드(buildFullGrid)는 [시간 그리드로 보기] 토글로 유지(sessionStorage tt_allview) — **반 통째 이동(빈 자리 클릭)은 그리드가 필요해서, 편집 '반 이동' 시작 시 자동으로 그리드 전환·끝나면 개요 복귀**(`allAutoGrid`). 학생 검색 바로가기는 개요면 그 반의 요일을 자동 확대(gotoSearchHit). 모바일 목록 보기는 그대로. 함수 접두 `all`(allView/buildAllOverview/buildAllZoom/allDayHeader).
  - **상단 메뉴 분리 — 메인 메뉴 / 시간표 작업 도구**(2026-08-22 사용자 확정, 샘플 이미지 승인 후 적용): 위 카드(#bar) = 메인 메뉴만 — 주차 배지 + 보기 탭(오늘/주차별/전체/캘린더/지필 — 개별 알약 버튼, 세그먼트 한 덩어리는 사용자가 모바일·컴퓨터 모두 분리 확정 2026-08-22) + '출석 관리' 묶음(`.cluster` — 순서·이름 사용자 확정 2026-08-22: [결석자 관리][학생 이력][결석/지각 확인]) + 새로고침 라인 SVG 아이콘(`.iconbtn`, 옛 '다시 불러오기' 글자 버튼 대체). 아래 카드(#sheetcard) = 시간표 + 그 화면의 작업 도구 — 도구줄(`.toolline`: 오늘=출석 도구(attbar), 전체/주차별=**편집 도구 접이식**(`.efold`/#editset, sessionStorage tt_editopen, 접으면 clearEdit) + 학생 검색) + 안내문·색 범례 한 줄(`.hintline`). `.sheet`는 #sheetcard 안의 일반 요소가 됨(카드 스타일은 #sheetcard로 이전). 전체 시간표는 카드 안 제목 줄 `.sheet-title`('전체 시간표' + **정규/내신 토글** + 오른쪽 [시간 그리드로 보기]) — 정규/내신은 전체 전용이라 메인 메뉴에서 뺌(2026-08-22 사용자 확정), 토글 on 상태는 렌더 때 book 값으로 직접 부여.
  - **학생 칩 = 테두리 없는 낮은 사각 태그**(2026-08-22 사용자 확정 시안 B — "알약 테두리가 겹겹이라 정신없음"): `.blk .stus button` 알약(radius 999·테두리) → radius 4px·border 없음(7px에서 사용자 요청으로 더 각지게, 2026-08-22)·반투명 흰 바탕, 상태는 바탕색만(출석 채움 초록/지각 #F1E2BB/결석 #EFD6D2 취소선/특이사항 #DFEAF7/1회 이동 #E7DDF3 보라 굵게). 호버·보존(kept)은 outline으로 표현. **출석 칩 색은 전 반 공통 저채도 톤**(2026-08-22 사용자 확정 — "고1 나 수업의 출석 컬러가 마음에 든다": 옛 .blk.c-b saturate(.35) 필터가 만들던 색을 계산해 고정 — 컴퓨터 saturate(.35) 톤·모바일(<760px) saturate(.75) 톤으로 분리(.65에서 한 번 더 상향) — 폰에선 낮은 채도가 지나치게 흐려 보여서(사용자 확정 2026-08-22)). '나'·'확인' 블록의 연한 배경은 필터 대신 색값 지정(.blk.c-b.c-g1 등 — 칩이 이중으로 바래지 않게), 선택(sel)·검색 강조는 원색 유지.
  - **이달의 신입생 카드**(2026-08-22 사용자 요청): **전체 시간표 아래** 카드 — 그 달 등록 신입생(이름·학교 학년·담당T·배정 시간표 가/나/진도/확인·등록일)을 최근 등록순으로, ◀▶로 다른 달 조회. 기존 `newStudents` 액션 재사용(superstar 신입 탭과 동일 데이터, 백엔드 변경 없음), 달별 페이지 내 캐시(`nsCache`) — [다시 불러오기] 때 함께 갱신. 등록일 기록은 2026-08-19부터라 그 전 등록생은 안 나옴(카드에 안내 문구). 함수 접두 `ns`, 스타일 `.ns-*`.
  - **등록일을 직접 넣고 고칠 수 있다**(2026-08-23 사용자 요청, 리포트 배포 @107): 슈퍼스타 관리 → 슈스 링크 → [수정] 맨 아래 **등록일** 칸 + [오늘로 넣기]. 이미 명단에 있던 학생을 그 달 신입으로 잡을 때 쓴다(반 배정만으로는 등록일이 생기지 않아 신입 카드에 안 뜬다 — 문세린 사례). 백엔드 `updateStudent`가 `regDate`를 **보낸 경우에만** '등록일' 열에 쓰고(텍스트 강제, 열이 없으면 만든다), `studentLinks` 응답에 `regDate`가 포함된다. 수파베이스 `students.reg_date` 미러도 함께 갱신. 비우고 저장하면 신입 표시가 사라진다.
- **오늘의 이슈 메모**: 오늘의 시간표 하단 메모칸(날짜당 1건, '시간표메모' 탭 A:날짜 B:메모 C:기록일시). 주차별 보기 하단 '주간 메모'에 그 주 7일치 누적 표시, 행 클릭으로 소급 작성·수정(빈 값 저장=삭제). API: GET ttMemoList&from&to / POST ttMemoSet{date,memo}.
  - **이미 처리된 출석 기록 보존 표시**(2026-08-20 사용자 확정 — "전체 시간표 수정 시 이미 처리된 시간표는 수정하지 않기"): 영구 이동·빼기로 현재 명단에서 빠진 학생도 그 날짜에 출석 기록이 있으면 오늘의 시간표·주차별 칸에 점선 칩('이동' 표기)으로 계속 표시, 클릭해 수정 가능(`keptAttendNames`/`keptChip`). 애초에 영구 이동은 출석기록 시트를 건드리지 않음 — 기록이 지워진 게 아니라 명단 기준 렌더링 때문에 안 보였던 것. 한계: 반대로 이동해 '들어온' 학생은 이동 전 지난 날짜 칸에도 새 반 명단으로 보인다(명단 이력은 미보관). 시작일이 미래인 이동은 이름 뒤 특이사항 표기("8/30부터")로 안내 권장.: 미완료 → [반이동][영상보충][무단 결석][당일 결석]. 반이동·무단·당일은 처리 즉시 완료. 영상보충은 일정 저장 시 '영상보충' 섹션으로 이동(plan이 '영상보충'으로 시작하면 그 섹션), 그 섹션의 [영상보충 출석] 버튼으로 완료. 건별 [메모] 버튼(약속 변경·태도 등) — '출석기록' 탭 J열 '보충메모', attendMakeupSet{plan,done,mkMemo}가 H~J 세 열을 항상 함께 씀(호출부는 mkMemo를 반드시 전달).
  - **결석자 관리는 정규·내신을 함께 표시**(2026-08-23 사용자 요청 — "주차별로 관리되는 게 아니라 미완료 친구들을 계속 다음 주차에도 확인할 수 있어야"): 예전에는 그 주 기간의 시간표(전역 `book`) 것만 조회해서, 주차가 정규→내신으로 바뀌면 지난 정규 미완료 건이 목록에서 사라졌다(8/26부터 내신 기간 → 정규 미완료 16건이 안 보일 상황이었음). 이제 `openMakeup`이 **정규+내신 두 시간표를 함께 조회해 한 목록**으로 보여주고(내신 건은 '(내신)' 표시), 각 항목에 `m.book`을 붙여 **처리·저장·삭제·반이동을 그 건의 시간표 기준**으로 수행한다(`mkClassesOf`/`classLabelOf(cid, bk)` — 다른 시간표 반 목록은 `cacheGet('list:…')` 재사용, 없으면 timetableList 1회 조회).
- **원본 데이터**: 리포트 스프레드시트의 **'정규시간표' 탭** (A:반ID B:요일 C:시작 D:끝 E:위치 F:담당T G:반이름 H:학생명단 공백구분) + '내신시간표'(동일 구조), '시간표이동기록', '출석기록' 탭.
- 학생 이동·추가·빼기·반이동 시 백엔드가 '학생정보' 탭의 정규가(G)/나(H) 열도 자동 갱신 → 학생 개별 페이지(s.html)·숙제 검사(hwcheck.html)에 즉시 반영.
- **숙제 검사 ↔ 시간표 연동**(2026-08-14): hwcheck 학생 카드에 그 주(수~일)의 출석/지각/결석·1회 이동 배지 표시 — timetableList+timetableWeek(정규)를 읽어 학생 가/나 시간대(slot) 일치 기록만 매칭(이름은 괄호 제거 후 비교).
  - 반이름 끝 '가'→정규가, '나'→정규나, 고3파이널·정리정독 등 단일 수업→정규가, '논술'은 학생정보 미반영. **내신 시간표는 학생정보에 반영하지 않음.**
- 구글시트 「★2026정규수업」은 슈의 참고용 원본 — **자동 동기화되지 않음**. 사용자가 "구글시트에서 시간표 다시 가져와줘" 하면: 시트 첫 탭을 읽어(병합셀 포함 파싱) `timetableSaveAll`로 전체 교체. (2026-08-14 세션에서 한 방법: clasp 로그인 → 백엔드에 임시 tmpDumpSheet 액션을 별도 배포로 추가해 시트 값+병합 읽기 → 파싱 → 임시 배포 삭제·코드 복구. 요일 헤더 행 2개(수목금/토일), 위치·담당T 행, 30분 시간축 구조.)

## 내신대비 피드백 (naeshin.html, 2026-08-15 구축)
구글시트 「내신 주차별 기록부」를 대체하는 페이지. 학습 관리 메뉴 '숙제 검사' 아래.
- **선택 흐름**: 시험 기간(26-2-중간/기말) → 학년 → 담당 강사 → 담당반. 강사를 고르면 라벨이 '○○T의 담당반'으로 바뀌고 담당반이 하나면 자동으로 열림. 학년·강사는 주소(?grade=&teacher=)에 저장 — 강사별 즐겨찾기용. 담당 강사 목록은 **선택한 학년의 반을 실제 맡은 강사만** 표시.
- **확인 수업 반은 제외**(반이름에 '확인' 포함) — 피드백은 진도 수업만 (사용자 확정).
- **명단은 내신 시간표(timetableList&book=내신)를 그대로 사용** — 별도 명단 없음. 시간표에서 학생을 추가/제거하면 새로고침 후 반영.
- **학생 검색**: 이름 입력 → 소속 진도반 버튼 → 그 반이 열리고 해당 학생 줄로 스크롤·강조.
- **시험범위 3칸**: 1.교과서 2.교과서 내 범위 3.교과서 외 범위. **2·3번은 한 줄에 한 항목(엔터 구분)** — 줄마다 진도 드롭다운 항목이 됨. (옛 '추가 자료' 칸은 삭제했으나 기존 기록은 보존·표시)
  - **학교·학년 단위 공유**: 저장 키가 `공유:고1|화정` 형태라 병렬 반(화정A/B/C)이 한 번만 입력하면 공유됨. 학교 구분 없는 반(정리정독·파이널 등)은 반 단위. 저장 즉시 진도 드롭다운에 반영(새로고침 불필요).
- **주차 = 슈국 캘린더의 내신 기간**(ttPeriodList에서 book='내신'인 수요일들). 드롭다운 표기 '2주 · 8/26~8/30', 기록 있으면 ●. 저장 키는 날짜(yyyy-MM-dd).
- **진도**: 시험범위 단원 **드롭다운으로 선택**(직접 타이핑하지 않음). 고르면 그 주 칩으로 들어가고 드롭다운에서 빠짐. 하다 만 단원은 다음 주에 다시 고르면 기간 확장(8/26~9/2). 칩 ×로 그 주에서만 빼기. 그 아래 칸은 **메모**(자유 기록).
- **과제**: 한 줄에 한 항목(엔터 구분) → 줄마다 학생별 버튼. 버튼을 누르면 창이 열려 **학습량(별5)·학습 깊이(별5)·미제출** 선택. 같은 별 재클릭=해제, 미제출과 별점은 상호배제. 미제출은 취소선 아닌 **붉은 '미' 배지**(취소선은 '다 했다'로 읽혀서 — 사용자 확정).
- **REPORT 버튼**(학생 번호·이름 아래): 캡쳐용 한 장 학습 보고서 — 시험범위(나간 단원 취소선+주차), 주차별 진도·과제(학생별 ✓학습량·깊이 / 미제출 / 미확인)·메모, 그 학생 기록. 기록 없는 주차는 생략.
- **저장 구조**: '내신기록' 탭 A:기간 B:반ID(또는 공유키) C:구분(범위/주차/학생/클리어) D:주차 E:학생 F:내용1 G:내용2 H:기록일시.
  - 범위 → F:JSON{book,inRange,extra,outRange} G:읽기용 문자열 / 주차 → F:메모 G:과제 / 학생 → F:메모 G:과제기록 JSON{과제명:{a,d,x}} / 클리어 → F:JSON{단원:{s,d}}
  - API: GET naeshinGet(period, classId, scopeKey) / POST naeshinSet(kind, week, student, text1, text2, scopeKey). 키 단위 덮어쓰기, 빈 값=삭제.
- **지필 리포트 연동**: 반 카드의 [지필 리포트 제작] → m.html에 시험범위·기간 자동 채움 + 반 이름에서 학교·학년 추정 + **같은 학교·학년 내신반 학생 자동 배정**('일부'). 명단 미일치 학생은 안내에 표시.
- **미룬 것**: 숙제 검사(hwcheck) 기능을 여기에 합치는 안 — 사용자가 "일단 지금 말고"(2026-08-15). 별점 구조가 비슷해 통합 시 자연스러움.

## 클리닉 — 강사 클리닉 단위 운영·중등 지원 (2026-08-21, 클리닉 배포 @17)
- **강사 클리닉 단위 운영**(2026-08-21 사용자 확정 — 공통 시간대·전체 신청받기 UI 제거): 모든 것이 강사별. clinic_assign.html 맨 위 **강사 선택이 페이지 전체 기준** — 시간대·신청받기(중단 토글)·신청 대상·배정 내역·신청 현황이 그 강사 클리닉으로 표시·저장. 스크립트 속성 `teacherSlots` [{teacher,slots,open,target}].
  - 배정 저장 = '배정도구' 기록에 item=강사 + 그 강사의 신청 대상(`setTarget&teacher=`) 저장. **item 빈 옛 배정·신청 기록은 전부 이수경T 클리닉으로 표시**(2026-08-21 사용자 확인, `LEGACY_ASSIGN_TEACHER`).
  - 학생 폼(index.html): 담당 강사(s.html이 ?tc=로 자동 전달, 변경 가능)에 따라 **그 강사의 열린 시간대만** 표시, 정원 9명도 강사별(`tcounts`), 학년 선택지도 그 강사 대상 학년(`teachers[].gradeTokens`). 설정 없는 강사의 학생은 신청 불가('준비되지 않았어요' / 서버 `bad_slot`), 중단 강사는 `teacher_closed`.
  - 신청 판정: 강사별 대상 있으면 그 대상, 없으면 공통(옛) 대상. s.html amITarget에 grade('중2' 형태)+teacher 전달. 신청 확인(teacher.html)에 담당 강사 필터.
  - 설정이 하나도 없으면 예전 공통 시간대 방식(구버전 호환). 옛 전체 open 플래그·setSlotMaster 등 액션은 백엔드에 호환용 유지.
- **중·고 학년 구분**: 학년을 '중2'/'고3' 형태로 매칭·기록(`gradeKey_`), 옛 숫자('3')는 고등 호환. 학생 폼 학년 선택지 중1~중3 추가. s.html은 amITarget에 `levelGrade()`('중2' 형태) 전달 — `hsGradeNum`(모의고사용, 고등 전용)과 별개.
- 주의: 수정 전에는 학년 대상 매칭이 숫자만 봐서 **'2026 중등 3학년' 대상이 고3에게 열려 있었다**(중3은 차단). 배포 후부터 설정 그대로 동작. 배정 페이지에서 학년 대상은 여러 학년 동시 지정 가능.

## 지필고사 리포트 — 시험 기간·제목 규칙 (2026-08-15)
- 제목 접두어 `26-2-중간` 형태가 **기간 구분의 공식 규칙**. 데이터 구조 변경 없이 제목에서 읽는다.
- **시험 등록(m.html)**: 기간 버튼 + 학교 드롭다운(roster의 학교) + 학년 드롭다운 + 과목명 입력 → `26-2-중간-화정고1-공통국어2` 자동 조합. 신규 등록은 기간 필수, 수정 모드는 제목·조합칸 잠금.
- **기간 버튼 목록은 `periodOptions()` 한 곳**(현재 26-2-중간, 26-2-기말만). **27년 시험은 연말에 사용자가 지시하면 여기에 추가**(사용자 확정 — 미리 넣지 말 것).
- **제출결과(t.html)**: [시험 기간] → [시험 선택] 2단계, 기본값은 가장 최근 기간. 접두어 없는 옛 시험은 '기간 없음'.
- **분석지 배정(analyses.html)**: 목록을 기간별로 묶어 최근 기간부터.

## 주말 실전 모의고사 — 통합형 (2026-08-15)
- **내년부터 전 학년이 선택과목 없는 통합형**(현 고1·고2식)을 쓴다. 올해 남은 고3 출제를 위해 두 형식 병행, **기본값은 고3형**.
- 출제(shueguk-hub `answer_key.html`) 상단 [통합형]/[고3형] 버튼. 통합형은 1~45 전체 공통, 영역 11개(화법·문법·인문예술·사회문화·과학기술·작문·현대시·현대소설·고전시가·고전소설·갈래복합), **1~6번 화법·7~10번 문법이 기본 지정**(변경 가능), 등급컷은 시험마다 1세트.
- 저장 데이터 `{ mode:'통합', 통합:{...} }` — 채점 백엔드가 이 키를 보면 선택과목 없이 채점(subject='공통'). 고3형 회차·기존 성적표는 그대로.
- 학생 OMR은 통합형 회차 선택 시 선택과목 버튼이 사라짐. 회차 목록은 `getExamList2`(이름+형식). 잘못 저장한 회차는 `deleteExam` 액션으로 삭제.
- **학생 메뉴 점검·수정(2026-08-22)**: s.html의 실전 모의고사 하위 메뉴(신청·OMR·성적) 세 가지 손봄.
  - 신청 페이지에 다녀와 돌아왔을 때(`refreshOnReturn`) 신청 내역은 갱신되는데 하위 메뉴는 다시 그리지 않아 '신청 완료' 표시가 한 박자 늦던 문제 — api('') 응답 처리에 `renderMockHub()` 추가(`loadGradeGates`에는 원래 있었음).
  - **신청받기 중단(open) 상태를 학생 화면에 표시**: `STATE.mockOpen`(days 응답 또는 리포트가 담아 준 `mockGates.open`) — 하위 '모의고사 신청' 카드가 '지금은 신청을 받지 않아요 / 신청 마감'으로 비활성, 메인 카드 설명은 'OMR 작성 · 성적 확인'. **메인 카드 자체는 계속 열어 둔다**(중단돼도 OMR·성적은 봐야 함). 값이 없으면(구버전·조회 실패) 옛 동작 유지, 이미 신청한 학생은 신청 내역 표시가 우선.
  - 신청 날짜 표기를 메인 카드와 같은 '8/22 (토)' 형식으로 통일(`mockDayLabel` 공용 — 신청 백엔드 `mySignups`와 리포트 `collectMockSignups_`가 같은 `{day,date}` 형식이라 그대로 씀).
- **모의고사 메뉴 접속지연 대책(2026-08-26)**: 메뉴를 여는 순간(s.html `openMockHub`→`prefetchMockMenu`) 두 가지를 선조회한다 — 백엔드 변경 없음, 화면만.
  - ① **성적**: `studentReports`(실측 2~15초)를 미리 받아 두고(`MOCK_PRE`) `submitPw`가 재사용 — 8자리를 입력하는 동안 도착해 체감 대기가 사라진다. **이름이 유일한 학생만**(id 없이도 name+school+uniq=1 조회가 8자리 조회와 결과 동일 — `studentReports_`의 idOk 규칙). 동명이인은 종전대로 입력 후 조회. 신선도 2분 — OMR 응시 직후 다녀오면 `refreshOnReturn`·재진입이 새로 받는다.
  - ② ~~신청 days sessionStorage 전달~~ — **같은 날 신청 데이터가 수파베이스 원본으로 전환되며 제거**(016 마이그레이션): signup.html이 `signup_days` 함수로 0.3초 안팎에 스스로 받는다. 자세한 것은 `supabase/README.md`·shuegukweekendtest CLAUDE.md.
  - ~~OMR 카드는 앱스스크립트 내부(iframe)라 선조회 불가~~ — **같은 날 출제·OMR도 수파베이스 원본으로 전환**(017 마이그레이션): hub `omr.html`이 정적 학생 OMR 본체가 되어 즉시 열리고, 제출·채점(0.5초 안팎)·성적 관리(`omr_teacher.html`)·s.html 모의고사 성적(`fetchMockData` — `omr_student_reports` 함수, 폴백 레거시) 모두 수파베이스. 자세한 것은 `supabase/README.md`. **모의고사 성적 탭 순서도 이때 고쳐짐**(옛 백엔드는 제출시각을 문자열로 정렬해 요일 이름 알파벳순이었다 — 이제 실제 시간순).
  - 검증(2026-08-26): 브라우저 E2E로 선조회 즉시 렌더→원본 승리→직접 접속 폴백→s.html 선조회 왕복(실 백엔드) 확인.

## H WORK 마감 (2026-08-15)
- HWORK목록 시트 **D열에 마감일**(yyyy-MM-dd, 텍스트). **마감일 밤 11:59까지 제출 가능, 다음 날부터 서버가 거절**(화면 우회 불가). 마감 확인은 캐시 사용.
- 과제 관리(hwork_assign)의 [마감일] 버튼으로 설정·변경·해제(빈 값=기한 없음). 학생 페이지는 마감 과제를 '(마감)'으로 선택 불가.
- 2026-08-15에 **기존 과제 8건 전부 마감 처리**(마감일 2026-08-14). 다시 열려면 마감일을 비우거나 미래 날짜로.

## 접속 지연 대책 (2026-08-19)
페이지 첫 화면의 서버 왕복 횟수와 시트 읽기 양을 줄였다. 배포 완료(리포트 @97, 어휘 @10).
- **통합 부팅 조회**: `ttBoot`(timetable.html — 시간표+오늘 출석+메모+주별 기간, 주차별이면 wfrom/wto로 주간 기록까지) / `hwcheckBoot`(hwcheck.html — 명단+검사 기록+정규 시간표+주간 출석). 부팅 4회 요청 → 1회. 기존 개별 액션은 전부 유지.
- **기록 시트 꼬리 읽기** `sheetTail_`: '출석기록'(G기록일시 기준, 여유 14일)·'시간표이동기록'(A기록일시 기준, 여유 60일)을 아래(최신)에서 필요한 만큼만 읽는다. attendSet·attendMakeupSet의 행 탐색도 동일. **주의: 기준일보다 14일(출석)/60일(이동) 넘게 앞서 미리 기록하는 기능을 만들면 여유를 함께 늘릴 것.** getAttendAbsentList(결석자 관리)는 오래된 미완료 건 때문에 전체 읽기 유지.
- **어휘 대시보드**: 목록은 `lite=1`(상세 제외, 1MB→압축 시 약 62KB), 상세는 `action=detail&row=&sig=`로 행별 조회, CSV 내보내기만 전체 조회. 어휘 Code.gs 배포본에 있던 taken 캐시·8/14 채점 보정 함수는 저장소로 회수함.
- 이 환경(원격 세션)의 프록시는 대용량 응답이 느리게 측정된다 — 실제 속도는 브라우저에서 확인할 것.
- **어휘 결과(티쳐스) 페이지 — 표를 나눠 그린다**(2026-08-24, 사용자 신고 "어휘 결과 페이지가 열리지 않아요"): 저장소는 `shueguk-voca/shueguk-teacher-dashboard.html`. 제출이 2,400건을 넘으면서 한 번에 모든 줄을 그리느라 휴대폰에서 데이터 도착 뒤 17초 더 멈췄다(첫 표까지 약 39초). 200줄씩 [더 보기]로 이어 붙이고 상세 줄은 누를 때 만들도록 고쳐 렌더 2.6초. **같은 구조를 쓰는 화면(출석 현황·변경사항 등)도 줄 수가 수천 건이 되면 같은 문제가 난다 — 줄마다 createElement+innerHTML+리스너 대신 문자열 한 번에 넣기·이벤트 위임·나눠 그리기.**
- **쓸 때 캐시를 통째로 비우지 말 것 — 꼬리만 읽어 이어 붙인다**(2026-08-24 어휘에서 확인, 어휘 배포 @13): 어휘 백엔드는 새 제출이 올 때마다 목록 캐시를 비웠다. 그래서 **학생이 제출하는 시간대 = 교사가 조회하는 그 시간**에는 캐시가 늘 비어 있어 매 조회가 시트 2,432줄 전체 읽기였고 6~84초가 걸렸다(캐시가 없는 것과 같았다). 캐시에 만들 당시의 마지막 행을 함께 적어 두고 그 뒤 늘어난 줄만 범위로 읽어 잇도록 바꾸니 **시트 읽기 2,433줄 → 0~7줄, 응답 2~4초**가 됐다. 목록은 다시 파싱하지 말고 JSON 문자열 끝에 붙이면 된다. 줄이 지워지면 행 번호가 밀리므로 그때는 통째로 다시 만든다(삭제만 캐시를 비운다).
  - **리포트 백엔드도 같은 구조다** — `attendSet`·`attendMakeupSet` 등 쓰기 액션이 캐시를 비우는 곳, 그리고 출석 현황처럼 기록이 쌓이는 화면에 그대로 적용된다. 출석기록이 수천 건이 되면 여기부터 볼 것.
  - **고칠 땐 반드시 가짜 시트로 왕복 검증**(SpreadsheetApp·CacheService·Utilities 스텁) — 제출·대량 제출·삭제 뒤 결과가 시트 전체를 다시 읽은 것과 완전히 같은지 대조한다. 꼬리 잇기는 조용히 어긋나기 쉽다.
- **어휘 lite 목록 캐시는 조각내 담는다**: CacheService 한 칸은 100KB. gzip+base64가 그 한계에 닿으면 `cache.put`이 조용히 실패해 **모든 조회가 시트 전체 읽기로 되돌아간다**(증상: 저녁에 느려짐). 어휘 Code.gs는 90KB씩 조각내도록 고쳤다(2026-08-24, 배포 완료). 리포트 쪽에서 큰 응답을 캐시할 때도 같은 한계를 확인할 것.
- **지연의 원인은 대개 전송량이 아니라 시트 읽기다**(2026-08-24): 어휘 목록은 압축하면 67KB인데 첫 바이트까지 6~84초였다. 느릴 때 응답을 줄이기 전에 **시트를 몇 줄 읽고 있는지** 먼저 볼 것.
- **2차(2026-08-19, 리포트 @100·신청 @12)**: 학생 페이지(s.html) 부팅 4~5회→2회 — getStudent 응답에 `mockGates`(신청 시트 '설정' 탭 미러에서 읽음, 60초 캐시)와 `vocaTaken`(어휘 스냅샷으로 자체 계산) 포함, 없으면 프런트가 직접 조회로 폴백. 클리닉 대상 조회만 페이지가 직접(설정이 클리닉 스크립트 속성에 있어서). '제출결과' 탭 60초 캐시(제출 시 dropExamIdx_가 무효화). superstar 별 더하기 탭 3회→1회(`starBoot`).
- **주의: 리포트 스크립트에서 UrlFetchApp을 쓰면 안 된다** — 소유자가 편집기에서 새 권한(외부 요청)을 승인하기 전까지 배포본에서 조용히 실패한다(2026-08-19 확인). 다른 프로젝트의 값이 필요하면 그 프로젝트가 자기 시트에 미러하게 하고 시트를 읽을 것(신청 게이트 방식).

## 시트 자동 변환 주의 (2026-08-15에 두 번 물린 문제)
구글시트가 `"8/16 일 11:00"`, `"2026-08-21"` 같은 문자열을 **날짜 값으로 자동 변환**해 저장하면, 코드가 텍스트로만 읽을 때 값을 놓친다(영어 날짜로 표시되거나 적용일을 무시하고 기록일로 후퇴).
- **쓸 때**: 문구·날짜 열은 `setNumberFormat('@')`로 텍스트 강제.
- **읽을 때**: 값이 Date 객체여도 받아들이도록 처리(`hwcheckTextStr_`, `ttApplyYmd_` 참고).

## 장기 계획: 데이터베이스 이전 (사용자 확정 방향, 2026-08-14 → **2026-08-22 시작됨**)
- **수파베이스 이전 진행 중** — 프로젝트 `shueguk`(`bangdbhqpphqqdwcledg.supabase.co`), 계획·진행 현황·키 취급 방침은 `supabase/README.md` 참고. Secret 키·sbp_ 토큰은 저장소에 절대 저장하지 않고 세션마다 사용자에게 받는다(publishable 키는 공개 가능).
- 언젠가 스프레드시트+Apps Script → 실제 DB(Firebase/Supabase 등)로 이전하기로 함. 시기는 한가한 때(방학 초 등)에 사용자가 지시.
- 기대 효과: 전 페이지 0.1~0.3초 응답, 실시간 반영(동시 출석 체크), 동시 수정 안전. 현 규모(≈500명)는 무료 요금제로 충분.
- 보완 조건: 시트 직접 열람을 대신할 관리 화면 또는 **DB→시트 일일 자동 백업**(사용자가 시트로 계속 볼 수 있게) 중 택1을 함께 구현할 것. 기존 데이터 이전은 자동 마이그레이션으로.
- 준비 상태: 페이지↔백엔드가 액션 단위 API로 분리되어 있어 연결부 교체 방식으로 이전 가능. 이 구조를 유지할 것.
- 그 전까지의 속도 대책(적용 완료): 병렬 로딩 + localStorage 캐시(즉시 표시 후 갱신) — timetable.html·hwcheck.html.

## 🚀 Apps Script(.gs) 재배포 방법 — 클로드(Claude)용 메모

이 계정(coke4497)의 Apps Script 백엔드는 **clasp**(구글 공식 CLI)로 명령줄에서 재배포할 수 있다.
사용자가 편집기에 복붙 후 수동 재배포할 필요 없이, 클로드가 `.gs`를 고치고 배포까지 한다.

**사용자는 비기술자다.** 지시는 아주 짧고 쉽게(“링크 클릭 → 허용 → 주소 붙여넣기” 수준)만 요청할 것.
사용자가 “앱스스크립트 배포해줘”라고 하면 아래 절차를 클로드가 알아서 수행한다.

### 전제
- 환경의 **네트워크 액세스 = 신뢰됨**이어야 googleapis 접속 가능(현재 OK).
- 이 환경엔 안전한 비밀 저장소가 없다(환경변수 칸은 공개됨). **토큰을 저장소/환경변수에 절대 넣지 말 것.** → 세션마다 아래 로그인 1회.

### 1) 설치
```bash
npm i -g @google/clasp   # 이미 있으면 생략
```

### 2) 로그인 (브라우저 필요 — 사용자 1분)
```bash
rm -f /tmp/co /tmp/cf; mkfifo /tmp/cf
( sleep 1800 > /tmp/cf ) &                                   # fifo write-end 유지
setsid bash -c 'clasp login --no-localhost < /tmp/cf > /tmp/co 2>&1' &
sleep 9; cat /tmp/co                                         # accounts.google.com URL 출력
```
- 출력된 **`https://accounts.google.com/...` URL**을 사용자에게 전달 → 사용자: **coke4497 계정으로** 열기 → 모두 **허용** → 리다이렉트된 **`http://localhost:8888/?...code=...` 주소 전체**를 복사해 전달.
- 받은 URL을 fifo로 전달해 완료:
```bash
printf '%s\n' '<사용자가-준-localhost-주소-전체>' > /tmp/cf
sleep 4; cat /tmp/co            # "Authorization successful" / ~/.clasprc.json 생성 확인
```
- 확인: `~/.clasprc.json`에 `tokens.default.refresh_token` 있으면 성공.

### 3) 배포 (프로젝트별)
```bash
mkdir -p /tmp/proj && cd /tmp/proj
printf '{"scriptId":"<SCRIPT_ID>"}' > .clasp.json
clasp pull -f                        # 현재 코드+appsscript.json 내려받기
cp <저장소의-최신-.gs> ./Code.gs     # pull 로 받은 코드 파일명에 맞춰 교체
clasp push -f
clasp list-deployments               # 배포 목록 확인 (구버전: clasp deployments)
clasp update-deployment -d "설명" <DEPLOYMENT_ID>   # 기존 배포 새 버전(= exec 주소 그대로 유지)
```
- **clasp 3.x부터 명령어가 바뀌었다**(2026-08-15 확인, 3.3.0 기준). 옛 `clasp deploy -i <ID>` → 지금은 **`clasp update-deployment <ID>`**(별칭 `redeploy`). 새 배포를 만드는 `clasp deploy`를 쓰면 주소가 바뀌어 학생 페이지가 깨지므로 절대 쓰지 말 것. 성공하면 `Redeployed <ID> @<새버전>` 이 출력된다.
- **덮어쓰기 전 반드시 확인**: `clasp pull -f`로 받은 현재 배포본과 저장소의 직전 커밋 버전을 `diff`로 비교해, 시트/편집기에서 직접 손댄 변경이 없는지 먼저 볼 것. 다르면 그대로 push 하면 남의 수정이 사라진다.
- **배포 후 동작 확인**: GET은 `curl -sL "<exec>?action=..."`로 되지만, **POST는 `curl -L`로 하면 HTML("Sorry, unable to open the file")이 돌아온다**(302에서 POST가 GET으로 바뀌어서). POST 확인은 리다이렉트를 직접 따라갈 것 —
  `loc=$(curl -s -o /dev/null -D - -X POST -H 'Content-Type: text/plain;charset=utf-8' -d '<json>' "<exec>" | grep -i '^location:' | tr -d '\r' | sed 's/^[Ll]ocation: //'); curl -s "$loc"`
- **DEPLOYMENT_ID** = 웹앱 주소 `/macros/s/`**`<이 부분>`**`/exec` 문자열(아래 표).
- **SCRIPT_ID**는 아직 미확보 → 처음 한 번 사용자에게 요청: “Apps Script 편집기 → ⚙️프로젝트 설정 → **스크립트 ID** 복사해서 알려주세요.” 알게 되면 아래 표에 채워 넣고 커밋해 둘 것.

### 프로젝트별 정보
| 프로젝트 | 저장소의 코드 파일 | DEPLOYMENT_ID (exec의 AKfycb… 부분) | SCRIPT_ID |
|---|---|---|---|
| OMR 채점 | `shuegukweekendtest/omr_code.gs` | `AKfycbyUHMdCH_u35Oeu6lEmx3yOYscoKLwEB8TC0QHGBOaCXZ4rbAnkMpP9_Na4l3QLOajGPA` | `1xfBA8bK-eBdosawcefdMOGm3dS3EC4oztM3q2rCQP_S3G41iXs54nEeG` |
| 주말 신청 | `shuegukweekendtest/signup_code.gs` | `AKfycbzdqac0xTnCaOo_t_2swJQqdfxjiA14sTo-ThTV8VvwcwaTucM1MQGeJfMfV4lNLM75` | `16G_RAaxC_g3LR5LwEjTgbqrKCaRS-FXa1FFWryVSRhyLEaOaHoz9Uzgp` |
| 리포트/공지 | `shueguk-report/backend-createReport.gs` | `AKfycbzhCncBwn-JlqXARC3wfrWUCuNHzlNK2df0bdhx-w78Xr8mzYUcIYZOJdRi9N4bHtsb` | `13qQ2fq-k8s_EAAhKjxEMN9GR_DpRp1VZDDV4SWGJS3DBVBgdM9hps2uP` |
| 어휘 | `shueguk-voca/apps-script/Code.gs` | `AKfycbxW4aXPUq9Iu3VtnjHuInUxhG6dPaNIYawDRWTYEjvEIsv4kjEceXDb_y9OX5Lx3-9e` | `1Q4Jrt1wVymBfWBb18_hUno4xI5VUD5NH2bLvZOtbEzCObBRQk5zjbhZe` |
| H WORK | `shueguk-h-work/apps-script/Code.gs` | `AKfycbyrc0j6EmVLrlfZeSchetAcEzWPEFAEV1RHStr6ZTJPDJeH6pcvRDemfWU64puojmSDPg` | `1bYZrb9ACZ-QAfsEjcpdL94rVbw_Y9dskaL1vkCJniVUGSGwHZ8U2VG1G` |
| 클리닉 | `shueguk-clinic/Code.gs` | `AKfycbw8e-054e4VUfRRx-PadyuoXRb-jxsKRcdOaq04SJH1oJyFVS_VOh9GUN_pL5cHPPzKVA` | `1NF3H9DL5mxiTeYyKXGs0MJOUB-jawH0YuekudKzPpapi0n7HRlFxCDq9` |

> **배포본이 저장소보다 앞서 있을 수 있다** — 2026-08-15에 H WORK·OMR 학생화면에서 실제로 그랬다. `clasp pull -f` 후 저장소 버전과 `diff`로 반드시 비교하고, 배포본이 최신이면 그것을 저장소로 먼저 가져올 것.
>
> 참고: 토큰은 세션마다 새로 로그인해 얻는다(저장 안 함). refresh_token 재사용은 안전한 비밀 저장소가 생기면 그때 도입.
